<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover">
    <link rel="stylesheet" href="/stylesheets/pico.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Todos</title>
    
    <style>
      /* Global CSS variables */
      :root {
        --spacing: 3rem;
        --text-weight: 400;
        --titles-weight: 800;
        --block-round: 0;
      }
      /* Typography */
      h2, h3, hgroup>:last-child {
        font-weight: 200;
      }
      small {
        color: var(--muted-text);
      }
      /* Header */
      
      
      
      
      
      :root:not([data-theme=dark]), [data-theme=light] {
          --nav-background: rgba(255, 255, 255, 0.7);
          --nav-border: rgba(115, 130, 140, 0.2);
      }
      [data-theme=dark] {
          --nav-background: rgba(16, 24, 30, 0.8);
          --nav-border: rgba(115, 130, 140, 0.2);
      }
    </style>
  </head>

  <body>
    <%- include('header'); -%>
    
    <div class="hero">
      <header class="container">
        <hgroup>
          <h1>Bienvenido</h1>
          <h2>
            <%- user.displayName -%>
          </h2>
          <p>Hemos enviado un email notificando su ingreso</p>
        </hgroup>
      </header>
    </div>
    
    <main class="container">
      
      
      <section>
      </section>
    </main>

  </body>
</html>